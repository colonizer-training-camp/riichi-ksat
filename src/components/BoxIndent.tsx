import styled from '@emotion/styled'
import View from './View'

const BoxIndent = styled(View)`
  border: 1px solid;
  padding: 1em;
  margin: 1em 0;
  text-indent: 1em;
  white-space: pre-wrap;
`

export default BoxIndent
