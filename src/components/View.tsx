import styled from '@emotion/styled'

const View = styled.fieldset`
  border: 1px solid;
  border-color: inherit;
  padding: 1em;
  margin: 1em 0;
  & > legend {
    margin: 0 auto;
    line-height: 1;
  }
`

export default View
